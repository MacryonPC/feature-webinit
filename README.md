# Webinit.sh 🚀

**webinit.sh** — это простой bash-скрипт для быстрого создания базовой структуры веб-проекта. Он автоматически создаёт папки и файлы, необходимые для старта разработки: HTML, CSS, JavaScript, а также папку для изображений. 

Скрипт написан с акцентом на **безопасность**: перед удалением существующей структуры запрашивается подтверждение, а само удаление выполняется с опцией `-i` (интерактивный режим), чтобы вы могли подтвердить каждый файл.

## 📁 Что создаётся

После запуска скрипта в текущем каталоге появится структура:

my-project/
├── image/
├── style/
│ └── style.css
├── script/
│ └── script.js
├── index.html
└── README.md



Имя главной папки можно задать при запуске (по умолчанию — `project`).

## 🛡️ Безопасность прежде всего

- **Никаких необратимых действий без спроса** — если папка с таким именем уже существует и содержит все ожидаемые файлы, скрипт спросит, хотите ли вы её удалить и создать заново.
- Удаление происходит через `rm -ri`, поэтому вы сможете подтвердить или отклонить удаление каждого отдельного файла.
- Скрипт **не использует `sudo`** и работает только в пределах текущего каталога.

## 🔧 Требования

- Любая Unix-подобная среда с `bash` (Linux, macOS).
- На Windows 11 можно использовать **WSL**, **Git Bash** или виртуальную машину.

## ⚙️ Установка

1. Скачайте файл `webinit.sh`:
   ```bash
   wget https://raw.githubusercontent.com/yourusername/yourrepo/main/webinit.sh


🚀 Использование

Запустите скрипт:
bash webinit.sh


📌 Версия и история изменений

Текущая версия: 0.0.7 (дата: 2026-02-15)

    0.0.7 — Исправлена критическая ошибка: переменные с путями к файлам определялись до ввода имени папки, что приводило к обращению к корневой директории (/). Теперь пути формируются после того, как имя папки получено от пользователя. Добавлена поддержка аргументов командной строки (--help, --dry-run), базовое наполнение файлов шаблонами, проверка наличия утилит.

    0.6 — Добавлена папка image, улучшена безопасность удаления, исправлены ошибки с путями.

    0.5 — Добавлен интерактивный ввод имени папки, диалог подтверждения удаления.

    0.4 — Базовая версия с созданием структуры и проверкой существования.


⚠️ Важные замечания (стоп-правила)

    Всегда проверяйте, в каком каталоге вы находитесь, перед запуском скрипта. Он создаёт папки и файлы в текущем рабочем каталоге.

    При использовании на Windows через Git Bash или WSL будьте особенно осторожны: случайное удаление системных файлов Windows возможно, если вы ошиблись путём. Работайте только в специально отведённых для проектов папках.

    Если вы не уверены, что хотите удалить существующую структуру, ответьте n на запрос. Вы всегда можете удалить файлы вручную позже.

📄 Лицензия

MIT — делайте с кодом что хотите, но я не несу ответственности за возможный ущерб. Будьте осторожны.


✨ Автор:Macryon
